[package]
name = "ide-lsp"
version = "0.1.0"
edition = "2021"
description = "LSP client: JSON-RPC transport and language server lifecycle"

[dependencies]
lsp-types = "0.97"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
