[package]
name = "ide-config"
version = "0.1.0"
edition = "2021"
description = "Settings, themes, and keymaps"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
